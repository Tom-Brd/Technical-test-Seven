<h2>Add Money to Wallet</h2>

<%= form_for @user, url: "/me/add", method: :patch do |f| %>
  <div class="field">
    <%= f.label :balance %><br>
    <%= f.number_field :balance, step: :any, required: true %>
  </div>

  <div class="actions">
    <%= f.submit "Add Money" %>
  </div>
<% end %>